#!/usr/bin/env bash

# set -euo pipefail
set -o errexit   # Same as -e: Exit on any command failure.
set -o nounset   # Same as -u: Exit when trying to use an unset variable.
set -o pipefail  # Same as -o pipefail: Pipeline fails if any command fails.



